[package]
description = "Wrust I/O"
name = "wrust_io"
version = "0.1.0"
authors = ["workanator <workanator@gmail.com>"]

[lib]
name = "wrust_io"
crate-type = ["rlib"]
test = true
plugin = false

[dependencies]
bytes = "*"
mio = "0.4"
libc = "*"
wrust_types = { path = "../types", version = "*" }
